
✅ Given I have a GitHub repo with a pull request 

And given I have a site on Pantheon with no corresponding branch (TBD of how I’ll check that. Probably by inspecting the logs of the full sh script.)

And those two repos have a shared history

When the build process runs for the PR

And then a CI env is made that contains a checkout of the GitHub repo

And then a new branch is checked out and created specific to this CI run

And then npm build runs (adding some comment in a css file about the build run that generated it. This could be checked later via curl)

And then the file generated by npm is committed

And node_modules is not committed. (Maybe add this assertion later)

Then a corresponding branch is created on the pantheon site

And then the CI environment adds the Pantheon repo as a remote

Then the CI environment checks out the branch from Pantheon

And then git merge the branch made specifically for this build, resolving all merge conflicts by accepting whatever was in the build branch.

Then push to Pantheon.

(Checking site, not logs)

Then check that the Pantheon remote site contains the CSS comment generated in npm build